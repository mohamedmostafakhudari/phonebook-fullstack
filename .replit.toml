modules = ["nodejs-24"]

# Development run button instructions
run = "cd backend && npm start"

[deployment]
# Production build and execution instructions
build = "cd backend && npm install && npm run build"
run = "cd backend && npm start"
ignorePorts = true