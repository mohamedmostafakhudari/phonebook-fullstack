{
	"name": "phonebook-backend",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "Mohamed Mostafa",
	"type": "commonjs",
	"main": "src/index.js",
	"scripts": {
		"start": "node src/index.js",
		"dev": "node --watch src/index.js",
		"build": "npm run build:ui",
		"build:ui": "rm -rf dist && cd ./frontend && npm install && npm run build && cp -r dist ../dist",
		"build:ui-old": "rm -rf dist && cd ../frontend && npm install && npm run build && cp -r dist ../backend",
		"deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"express": "^5.2.1",
		"morgan": "^1.11.0"
	}
}
